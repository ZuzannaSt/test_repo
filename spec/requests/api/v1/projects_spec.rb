describe "Projects API" do 
  # it 'not #authorised without token' do
  #   get '/api/v1/project/index'
  #   #expect(JSON.parse(reponse.body)).to_be("HTTP Token: Access denied.") 
  # end

  # it '#authorised with token'
end
